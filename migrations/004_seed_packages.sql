-- ============================================================
-- 004_seed_packages.sql
-- Auto-generated by scripts/parse-catalog.js
-- Run AFTER 003_seed_subjects.sql AND after scraper has run
-- ============================================================

-- Helper: add unique constraint on packages if not exists
-- (safe to run multiple times)
DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1 FROM pg_constraint WHERE conname = 'packages_program_id_semester_key'
  ) THEN
    ALTER TABLE packages ADD CONSTRAINT packages_program_id_semester_key UNIQUE (program_id, semester);
  END IF;
END $$;

-- Packages for program 53 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '53' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '53' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '53' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '53' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '53' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '53' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '53' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 8 — ' || p.name,  'Paket bahan ajar semester 8 Program Studi ' || p.name,  8, true FROM programs p WHERE p.code = '53' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 53 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '53' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 53 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '53' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 53 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '53' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 53 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '53' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 53 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '53' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 53 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '53' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 53 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '53' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 53 semester 8
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '53' AND pk.semester = 8
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 54 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '54' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '54' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '54' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '54' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '54' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 54 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '54' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 54 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '54' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 54 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '54' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 54 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '54' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 54 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '54' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 55 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '55' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '55' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '55' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '55' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '55' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '55' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '55' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 8 — ' || p.name,  'Paket bahan ajar semester 8 Program Studi ' || p.name,  8, true FROM programs p WHERE p.code = '55' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 55 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '55' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 55 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '55' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 55 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '55' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 55 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '55' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 55 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '55' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 55 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '55' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 55 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '55' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 55 semester 8
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '55' AND pk.semester = 8
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 56 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '56' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '56' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '56' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '56' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '56' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '56' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '56' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 56 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '56' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 56 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '56' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 56 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '56' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 56 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '56' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 56 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '56' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 56 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '56' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 56 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '56' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 57 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '57' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '57' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '57' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '57' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '57' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '57' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '57' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 57 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '57' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 57 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '57' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 57 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '57' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 57 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '57' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 57 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '57' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 57 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '57' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 57 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '57' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 58 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '58' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '58' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '58' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '58' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '58' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '58' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '58' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 8 — ' || p.name,  'Paket bahan ajar semester 8 Program Studi ' || p.name,  8, true FROM programs p WHERE p.code = '58' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 58 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '58' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 58 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '58' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 58 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '58' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 58 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '58' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 58 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '58' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 58 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '58' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 58 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '58' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 58 semester 8
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '58' AND pk.semester = 8
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 59 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '59' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '59' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '59' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '59' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '59' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '59' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '59' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 8 — ' || p.name,  'Paket bahan ajar semester 8 Program Studi ' || p.name,  8, true FROM programs p WHERE p.code = '59' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 59 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '59' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 59 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '59' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 59 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '59' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 59 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '59' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 59 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '59' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 59 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '59' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 59 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '59' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 59 semester 8
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '59' AND pk.semester = 8
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 60 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '60' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '60' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '60' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '60' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '60' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '60' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 60 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '60' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 60 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '60' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 60 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '60' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 60 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '60' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 60 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '60' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 60 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '60' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 61 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '61' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '61' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 61 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '61' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 61 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '61' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 62 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '62' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '62' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '62' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '62' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '62' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '62' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '62' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 62 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '62' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 62 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '62' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 62 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '62' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 62 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '62' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 62 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '62' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 62 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '62' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 62 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '62' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 63 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '63' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '63' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '63' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '63' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '63' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '63' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '63' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 63 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '63' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 63 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '63' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 63 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '63' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 63 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '63' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 63 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '63' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 63 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '63' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 63 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '63' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 64 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '64' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '64' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '64' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '64' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '64' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '64' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '64' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 64 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '64' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 64 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '64' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 64 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '64' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 64 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '64' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 64 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '64' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 64 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '64' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 64 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '64' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 65 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '65' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '65' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '65' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '65' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '65' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '65' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '65' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 8 — ' || p.name,  'Paket bahan ajar semester 8 Program Studi ' || p.name,  8, true FROM programs p WHERE p.code = '65' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 65 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '65' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 65 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '65' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 65 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '65' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 65 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '65' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 65 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '65' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 65 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '65' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 65 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '65' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 65 semester 8
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '65' AND pk.semester = 8
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 66 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '66' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '66' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '66' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '66' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '66' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '66' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '66' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 8 — ' || p.name,  'Paket bahan ajar semester 8 Program Studi ' || p.name,  8, true FROM programs p WHERE p.code = '66' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 66 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '66' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 66 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '66' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 66 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '66' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 66 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '66' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 66 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '66' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 66 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '66' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 66 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '66' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 66 semester 8
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '66' AND pk.semester = 8
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 67 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '67' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '67' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '67' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '67' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '67' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '67' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '67' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 67 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '67' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 67 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '67' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 67 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '67' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 67 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '67' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 67 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '67' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 67 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '67' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 67 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '67' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 68 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '68' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '68' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '68' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '68' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '68' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '68' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '68' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 68 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '68' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 68 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '68' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 68 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '68' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 68 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '68' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 68 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '68' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 68 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '68' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 68 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '68' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 69 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '69' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '69' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '69' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '69' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '69' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '69' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '69' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 8 — ' || p.name,  'Paket bahan ajar semester 8 Program Studi ' || p.name,  8, true FROM programs p WHERE p.code = '69' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 69 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '69' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 69 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '69' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 69 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '69' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 69 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '69' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 69 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '69' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 69 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '69' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 69 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '69' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 69 semester 8
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '69' AND pk.semester = 8
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 70 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '70' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '70' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '70' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '70' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '70' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '70' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 70 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '70' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 70 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '70' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 70 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '70' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 70 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '70' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 70 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '70' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 70 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '70' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 71 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '71' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '71' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '71' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '71' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '71' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '71' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '71' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 8 — ' || p.name,  'Paket bahan ajar semester 8 Program Studi ' || p.name,  8, true FROM programs p WHERE p.code = '71' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 71 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '71' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 71 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '71' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 71 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '71' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 71 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '71' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 71 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '71' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 71 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '71' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 71 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '71' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 71 semester 8
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '71' AND pk.semester = 8
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 72 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '72' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '72' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '72' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '72' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '72' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 72 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '72' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 72 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '72' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 72 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '72' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 72 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '72' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 72 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '72' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 73 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '73' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '73' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '73' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '73' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '73' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '73' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '73' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 73 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '73' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 73 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '73' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 73 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '73' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 73 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '73' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 73 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '73' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 73 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '73' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 73 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '73' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 74 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '74' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '74' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '74' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '74' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '74' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '74' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 74 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '74' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 74 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '74' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 74 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '74' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 74 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '74' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 74 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '74' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 74 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '74' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 75 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '75' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '75' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '75' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '75' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '75' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '75' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '75' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 75 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '75' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 75 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '75' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 75 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '75' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 75 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '75' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 75 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '75' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 75 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '75' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 75 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '75' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 76 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '76' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '76' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '76' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '76' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '76' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '76' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '76' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 8 — ' || p.name,  'Paket bahan ajar semester 8 Program Studi ' || p.name,  8, true FROM programs p WHERE p.code = '76' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 76 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '76' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 76 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '76' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 76 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '76' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 76 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '76' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 76 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '76' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 76 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '76' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 76 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '76' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 76 semester 8
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '76' AND pk.semester = 8
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 77 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '77' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '77' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '77' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '77' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '77' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '77' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '77' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 77 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '77' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 77 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '77' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 77 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '77' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 77 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '77' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 77 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '77' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 77 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '77' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 77 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '77' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 78 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '78' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '78' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '78' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '78' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '78' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 78 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '78' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 78 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '78' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 78 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '78' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 78 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '78' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 78 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '78' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 79 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '79' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '79' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '79' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '79' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '79' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '79' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '79' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 8 — ' || p.name,  'Paket bahan ajar semester 8 Program Studi ' || p.name,  8, true FROM programs p WHERE p.code = '79' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 79 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '79' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 79 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '79' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 79 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '79' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 79 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '79' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 79 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '79' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 79 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '79' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 79 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '79' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 79 semester 8
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '79' AND pk.semester = 8
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 80 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '80' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '80' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '80' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '80' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '80' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 80 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '80' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 80 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '80' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 80 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '80' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 80 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '80' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 80 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '80' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 81 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '81' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '81' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '81' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '81' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '81' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '81' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '81' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 81 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '81' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 81 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '81' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 81 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '81' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 81 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '81' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 81 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '81' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 81 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '81' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 81 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '81' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 82 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '82' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '82' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '82' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '82' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '82' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '82' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '82' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 82 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '82' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 82 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '82' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 82 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '82' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 82 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '82' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 82 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '82' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 82 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '82' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 82 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '82' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 83 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '83' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '83' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '83' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '83' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '83' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '83' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '83' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 83 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '83' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 83 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '83' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 83 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '83' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 83 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '83' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 83 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '83' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 83 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '83' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 83 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '83' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 84 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '84' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '84' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '84' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '84' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '84' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '84' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 84 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '84' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 84 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '84' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 84 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '84' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 84 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '84' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 84 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '84' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 84 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '84' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 85 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '85' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '85' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '85' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '85' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '85' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '85' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 85 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '85' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 85 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '85' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 85 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '85' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 85 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '85' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 85 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '85' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 85 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '85' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 86 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '86' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '86' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '86' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '86' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '86' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 86 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '86' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 86 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '86' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 86 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '86' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 86 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '86' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 86 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '86' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 87 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '87' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '87' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '87' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '87' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '87' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '87' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '87' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 87 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '87' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 87 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '87' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 87 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '87' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 87 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '87' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 87 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '87' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 87 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '87' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 87 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '87' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 88 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '88' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '88' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '88' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '88' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '88' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '88' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 88 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '88' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 88 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '88' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 88 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '88' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 88 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '88' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 88 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '88' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 88 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '88' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 89 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '89' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '89' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '89' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '89' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '89' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '89' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '89' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 8 — ' || p.name,  'Paket bahan ajar semester 8 Program Studi ' || p.name,  8, true FROM programs p WHERE p.code = '89' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 89 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '89' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 89 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '89' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 89 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '89' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 89 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '89' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 89 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '89' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 89 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '89' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 89 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '89' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 89 semester 8
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '89' AND pk.semester = 8
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Packages for program 90 ────────────────────────────────
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 1 — ' || p.name,  'Paket bahan ajar semester 1 Program Studi ' || p.name,  1, true FROM programs p WHERE p.code = '90' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 2 — ' || p.name,  'Paket bahan ajar semester 2 Program Studi ' || p.name,  2, true FROM programs p WHERE p.code = '90' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 3 — ' || p.name,  'Paket bahan ajar semester 3 Program Studi ' || p.name,  3, true FROM programs p WHERE p.code = '90' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 4 — ' || p.name,  'Paket bahan ajar semester 4 Program Studi ' || p.name,  4, true FROM programs p WHERE p.code = '90' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 5 — ' || p.name,  'Paket bahan ajar semester 5 Program Studi ' || p.name,  5, true FROM programs p WHERE p.code = '90' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 6 — ' || p.name,  'Paket bahan ajar semester 6 Program Studi ' || p.name,  6, true FROM programs p WHERE p.code = '90' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 7 — ' || p.name,  'Paket bahan ajar semester 7 Program Studi ' || p.name,  7, true FROM programs p WHERE p.code = '90' ON CONFLICT (program_id, semester) DO NOTHING;
INSERT INTO packages (program_id, name, description, semester, is_active) SELECT p.id,  'Paket Semester 8 — ' || p.name,  'Paket bahan ajar semester 8 Program Studi ' || p.name,  8, true FROM programs p WHERE p.code = '90' ON CONFLICT (program_id, semester) DO NOTHING;

-- Package modules: program 90 semester 1
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '90' AND pk.semester = 1
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 90 semester 2
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '90' AND pk.semester = 2
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 90 semester 3
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '90' AND pk.semester = 3
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 90 semester 4
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '90' AND pk.semester = 4
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 90 semester 5
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '90' AND pk.semester = 5
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 90 semester 6
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '90' AND pk.semester = 6
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 90 semester 7
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '90' AND pk.semester = 7
ON CONFLICT (package_id, module_id) DO NOTHING;

-- Package modules: program 90 semester 8
INSERT INTO package_modules (package_id, module_id, sort_order)
SELECT pk.id, m.id,
  ROW_NUMBER() OVER (PARTITION BY pk.id ORDER BY s.code)::int
FROM packages pk
JOIN programs p ON pk.program_id = p.id
JOIN subjects s ON s.program_id = p.id AND s.semester_hint = pk.semester
JOIN subject_modules sm ON sm.subject_id = s.id
JOIN modules m ON m.id = sm.module_id
WHERE p.code = '90' AND pk.semester = 8
ON CONFLICT (package_id, module_id) DO NOTHING;
